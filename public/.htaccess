# Redirect all requests to index.php (for Apache)
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /librava/
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^ index.php [L]
</IfModule>
